/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Certeza_idea',
  assetPrefix: '/Certeza_idea/',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
